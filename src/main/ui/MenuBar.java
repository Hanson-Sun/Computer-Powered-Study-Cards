package ui;

import javax.swing.*;
import java.awt.*;

// Stylized JMenuBar
public class MenuBar extends JMenuBar {

    // EFFECTS: instantiate new JMenuBar
    public MenuBar() {
        super();
        this.setBackground(Color.white);
        this.setMargin(null);
    }
}
